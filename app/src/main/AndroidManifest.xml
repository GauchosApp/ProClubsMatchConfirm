<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="m2wapps.ar.proclubsmatchconfirm">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:name=".Analytics"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activities.StartActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Activities.PartidoActivity"
            android:label="@string/app_name"
            />
        <activity
            android:name=".Activities.ListaActivity"
            android:label="@string/app_name"
            />
        <activity
            android:name=".Activities.MainActivity"
            android:label="@string/app_name"
            />
        <activity
            android:name=".Activities.ResultadosActivity"
            android:label="@string/app_name"
            />
        <activity
            android:name=".Activities.PosicionesActivity"
            android:label="@string/app_name"
            />
        <activity android:name=".Activities.RankingsActivity"
            android:label="@string/app_name"            />


    </application>

</manifest>